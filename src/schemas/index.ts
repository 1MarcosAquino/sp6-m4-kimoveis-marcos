import {
    user,
    userResquest,
    userResponse,
    userLogin,
    creaeToken,
    userUpdate,
} from './users.schemas';

import { category, categoryRequest } from './category.schemas';

import { realEstate, realEstateRequest } from './realEstate.schemas';

import { address } from './addresses.schemas';

import { schedulesRequest, schedules } from './schedules.schemas';

export default {
    user,
    userResquest,
    userResponse,
    userLogin,
    creaeToken,
    userUpdate,
    category,
    categoryRequest,
    realEstate,
    realEstateRequest,
    address,
    schedulesRequest,
    schedules,
};
